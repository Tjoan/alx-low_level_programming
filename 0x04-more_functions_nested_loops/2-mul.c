#include "main.h"

/**
* mul - multiplies two integers.
*
* @a: fisr variable
* @b: second variable
*
* Description: function that multiplies two integers.
*
* Return: mulitplied value of two integers
*/
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
